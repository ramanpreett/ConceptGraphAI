# Copy this file to .env and fill in your values. Do NOT commit .env to source control.
# Example MongoDB Atlas connection string (replace <username> and <password>):
MONGODB_URI="mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/conceptgraph?retryWrites=true&w=majority"

# Server
PORT=5000
